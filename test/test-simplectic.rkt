#lang scheme/base

;; 1. Start with a simple hamiltonian: a harmonic oscillator.
(define (ham p q) (+ (* p p) (* q q)))

;; 2. Compute partial differentials using autodiff to yield differential equations.

;; 3. Approximate using a symplex step

;; 4. Compute invariant?



